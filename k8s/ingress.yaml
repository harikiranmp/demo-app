apiVersion: v1
kind: Service
metadata:
  name: demo-app-svc
  labels:
    app: demo-app
spec:
  type: LoadBalancer
  selector:
    app: demo-app
  ports:
    - port: 80              # External port (publicly exposed)
      targetPort: 3000      # Port your container listens on
      protocol: TCP
